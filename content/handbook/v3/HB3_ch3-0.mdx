---
title: "Menota handbook ch. 3 (v. 3.0): Document structure"
author:
 - Matthew Driscoll
 - Rune Kyrkjebø
 - Odd Einar Haugen
---

# Chapter 3. Document structure

Version 3.0 (12 December 2019)

This version has been superseded by [version 3.1](HB3_ch3.xml#) (19 July 2020)

by Matthew Driscoll, Rune Kyrkjebø and Odd Einar Haugen
## 3.1 The structure of the manuscript vs. the structure of the work

Viewed as physical objects, rather than as vehicles for texts, manuscripts have a
     certain structural hierarchy. What is regarded as a single manuscript may in fact comprise more
     than one volume; _Flateyjarbók_, for example, is bound in two volumes, and the large rímur codex
     Acc. 22 in three. A manuscript book is made up of quires or gatherings, each of which contains
     a number of leaves, normally eight. Each leaf has a recto side and a verso side, and each side
     may be further divided into columns. The text is then written in lines across the page or
     column. In order to be able to locate a word quickly and easily, all, or at least most, of
     these structural divisions must be registered. We need to know that a given word appears e.g.
     in the fifth line of the right-hand or b column on the recto side of folio 34. As it is
     customary to foliate manuscripts without regard to their quire division, the quires will not
     normally need to be included in the hierarchical structure, but since the quiring can have
     implications for the text itself this division should be indicated, and will also generally
     form part of the `<msDesc>{:xml}` element, found in the document header.

At the same time, of course, manuscripts obviously do contain texts, which is the reason why most of us are interested in them in the first place. A single manuscript will often contain more than one work, each of which may, in the case of lengthy prose works such as sagas, be divided into chapters or sections. In the case of Medieval Icelandic poetry, <em>rímur</em> for example, a single work (<em>rímnaflokkur</em>) will usually consist of several cantos or fits, each containing a number of stanzas, made up of a number of lines. It may be necessary to group these lines in some other ways as well. The stanzas comprising the <em>mansöngur</em> should be distinguished from the main body of the fit, for example, while to facilitate certain types of metrical analysis it might be desirable to divide the individual stanzas into couplets. Some types of poetry, such as the <em>vikivakakvæði</em>, will have a refrain or burden, which should ideally also be distinguished from the narrative section(s) of the stanza.

XML has at its foundation the notion of a text as a single hierarchical structure, which means that it does not work well where there are several concurrent hierarchies, as is obviously the case when one wishes for example to indicate the line divisions both in a poem and in the manuscript in which the poem is contained. The [TEI
               Guidelines](http://www.tei-c.org/Guidelines/P5/) offer various solutions to this problem, enabling both the structure of the document and the structure of the text to be encoded.
### 3.1.1 Hierarchical divisions

The principal means of representing hierarchy is the `<div>{:xml}` (i.e. “division”) element. `<div>{:xml}` elements may freely nest within each other. The `<div>{:xml}` element has, in addition to the universally available `@id{:sh}` and `@n{:sh}` attributes, a `@type{:sh}` attribute, which specifies the name conventionally given to the level of division, e.g. `"chapter"{:js}`, `"stanza"{:js}`, `"couplet"{:js}`, if attempting to represent the structure of the text, or `"page"{:js}`, `"column"{:js}`, `"line"{:js}` if the physical structure of the manuscript is to be preferred. It will be convenient to specify a value for the `@type{:sh}` attribute in the `<div>{:xml}` element at least each time a change of level occurs. The software, however, will keep count of the levels of nesting even if the type attribute is not used.

The complex structure of a work such as a set of rímur could be represented by using four levels of `<div>{:xml}` elements, `<div type="canto">{:xml}` for the cantos or fits, `<div type="part">{:xml}` for the parts (for example the <em>mansöngvar</em>), `<div type="stanza">{:xml}` for the stanzas, and `<div type="line">{:xml}` for the lines. If the manuscript being encoded contains more than one set of rímur, as is frequently the case, it might be sensible to use `<div type="canto">{:xml}` for each set. A simpler form of mark-up is possible, however. Instead of `<div>{:xml}` elements, the `<l>{:xml}` element (for “line”) and the `<lg>{:xml}` element (for “line-group”, i.e. a group of lines functioning as a formal unit) can be used, reserving the `<div>{:xml}` element for larger structural units. The `@type{:sh}` attribute is then used to identify the type of unit, e.g. `"stanza"{:js}`, `"couplet"{:js}`, like in `<lg type="stanza">{:xml}`. Here again the type need
                  only be defined once. Lines and line-groups can also be numbered and identified using the `@n{:sh}` and `@id{:sh}` attributes.

This type of markup focusses on the hierarchical structure of the text. The actual physical realisation of the text is considered of secondary importance – if of importance at all – when dealing with modern printed literary works: little significance is attached to the page and line beginnings in the various editions of, say, Orwell’s _Nineteen Eighty-Four_. In some cases, however, the early editions of Joyce’s works, for example, supervised by the author himself, the physical make-up of the text can be of great consequence. It may also be necessary to maintain the pagination and lineation of standard editions of major works, as these are frequently used in citations in scholarly works. In the case of chirographically transmitted material, the physical organisation of the text is more likely to be recognised as being of importance and in need of encoding. This can be done hierarchically, as above, using `<div>{:xml}` elements, which are then
                  given the appropriate `@type{:sh}` attributes, e.g. `"page"{:js}`, `"column"{:js}` or `"line"{:js}`, but it seems more appropriate to reserve these elements for structural divisions in the text, while indicating the physical structure of the document through the use of so-called “milestone” elements, i.e. `<pb/>{:xml}`, `<cb/>{:xml}` and `<lb/>{:xml}`. These elements make up a separate hierarchy in the file and help to overcome the problem of overlapping structures in the mark-up; see also the discussion in [ch. 16](HB3_ch16.xml) below.

The rest of this chapter presents how the text may be encoded at higher structural levels than characters and words. Important elements here are the larger divisions of the text, like chapters, paragraphs (with headings), and stanzas. This chapter also presents how pagination and foliation, together with column and line beginnings, may be encoded. The following [TEI](http://www.tei-c.org/release/doc/tei-p5-doc/en/html/REF-ELEMENTS.html) elements are presented:

| Elements | Contents |
|-----|-----|
| `<text>{:xml}`, `<body>{:xml}` | Main divisions of the text, |
| `<div>{:xml}` | division into chapters (multiple levels are encoded by nesting elements), |
| `<p>{:xml}` | prose paragraphs, |
| `<w>{:xml}` | words, |
| `<pc>{:xml}` | punctuation marks, |
| `<lg>{:xml}`, `<l>{:xml}` | line groups and lines (for stanzas), |
| `<head>{:xml}` | headings, |
| `<pb/>{:xml}`, `<cb/>{:xml}`, `<lb/>{:xml}` | page, column and line beginnings. |


We will also discuss a few other elements which are relevant for the document structure:

| Elements | Contents |
|-----|-----|
| `<c>{:xml}` | Any character of note, |
| `<q>{:xml}` | direct speech, |
| `<quote>{:xml}` | quotations from external sources, |
| `<choice>{:xml}` | grouping of alternative encoding, |
| `<handShift/>{:xml}` | change of scribal hand. |

## 3.2 Main divisions of a TEI document

The following presentation is based on the TEI P5 Guidelines, [ch. 4](http://www.tei-c.org/release/doc/tei-p5-doc/en/html/DS.html).

A TEI document is always at its highest level enclosed by the start tag `<TEI>{:xml}` and the end tag `</TEI>{:xml}`. Within the `<TEI>{:xml}` element, two other elements appear in a fixed order, namely the `<teiHeader>{:xml}` and the `<text>{:xml}` elements. Within the `<text>{:xml}` element, the body text may appear, enclosed in the element `<body>{:xml}`. If the text has front matter, there will be an element `<front>{:xml}`, placed before `<body>{:xml}` containing it. Similarly, there may be an element `<back>{:xml}`, placed after `<body>{:xml}` and containing back matter. The elements `<teiHeader>{:xml}`, `<text>{:xml}` and `<body>{:xml}` are required in any TEI-conformant document, while `<front>{:xml}` and `<back>{:xml}` are optional. This, then, is the basic structure of a TEI document:

| Elements | Contents |
|-----|-----|
| `<TEI>{:xml}` | The TEI document begins here, |
| `<teiHeader>{:xml}` . . . `</teiHeader>{:xml}` | the header goes here, |
| `<text>{:xml}` | the text itself begins here, |
| `<front>{:xml}` . . . `</front>{:xml}` | any front matter goes here, |
| `<body>{:xml}` . . . `</body>{:xml}` | the main body of the text goes here, |
| `<back>{:xml}` . . . `</back>{:xml}` | any back matter goes here, |
| `</text>{:xml}` | the text ends here, |
| `</TEI>{:xml}` | the TEI document ends here. |

### 3.2.1 Another possible first division of the text: More than one &lt;text&gt; element

The transcriber may want to divide a document into more than one text. This can be done with the `<group>{:xml}` element, which should be contained in the top level `<text>{:xml}` element taking the place of `<body>{:xml}` in the simpler scheme illustrated above. The following structure appears:
```xml
<text xml:lang="en">
  <front>. . .</front>
  <group>
    <text xml:lang="en">
      <front>. . .</front>
      <body>. . .</body>
      <back>. . .</back>
    </text>
    <text xml:lang="en">
      <front>. . .</front>
      <body>. . .</body>
      <back>. . .</back>
    </text>
  </group>
  <back>. . .</back>
</text>
```


The main structure of the text, at the levels of work, first main division, second main division, first chapter of first main division, second chapter of first main division and so on, can be encoded in different ways. If the electronic document consists of more than one work, the `<group>{:xml}` structure illustrated above is the natural choice. In that case, one would get multiple sets of further structural divisions, one set within each of the `<body>{:xml}` elements. If the electronic document is considered as a single work, and placed in one `<text>{:xml}` element, there will only be a single `<body>{:xml}` element that needs further divisions.

Each major section of a text, `<front>{:xml}`, `<body>{:xml}` and `<back>{:xml}`, must have a minimum of contents, such as one or more `<div>{:xml}` elements for chapters, and within each `<div>{:xml}` element, one or more `<p>{:xml}` elements containing paragraph text.
## 3.3 Chapters: &lt;div&gt;

Further division of the `<body>{:xml}` block is achieved through `<div>{:xml}` elements, with one level nesting inside the other as the transcriber moves down through the hierarchical structure of the text.
### 3.3.1 Type- and level-specified &lt;div&gt; elements

In a complex document, `<div>{:xml}` elements may be specified by `@type{:sh}` and `@n{:sh}` attributes. In this example, the three first chapters of a work have been contained in `<div>{:xml}` elements at the same hierarchical level (siblings):

| Elements | Contents |
|-----|-----|
| `<div type="chapter" n="1">{:xml}` . . . `</div>{:xml}` | Chapter one goes here, |
| `<div type="chapter" n="2">{:xml}` . . . `</div>{:xml}` | chapter two goes here, |
| `<div type="chapter" n="3">{:xml}` . . . `</div>{:xml}` | chapter three goes here (and so on). |

### 3.3.2 Unspecified &lt;div&gt; elements

It is also possible to use `<div>{:xml}` elements without specifying their type:

| Elements | Contents |
|-----|-----|
| `<div>{:xml}` . . . `</div>{:xml}` | Chapter one goes here, |
| `<div>{:xml}` . . . `</div>{:xml}` | chapter two goes here, |
| `<div>{:xml}` . . . `</div>{:xml}` | chapter three goes here (and so on). |

### 3.3.3 Nesting &lt;div&gt; elements

Note that `<div>{:xml}` elements may nest inside each other. For example, the levels of work, chapter and then paragraph can be encoded in the following manner:

| Elements | Contents |
|-----|-----|
| `<div type="work">{:xml}` | The whole work starts here, |
| `<div type="chapter">{:xml}` | the first subdivision starts here (nested), |
| `<p>{:xml}` . . . `</p>{:xml}` | one paragraph of the subdivision goes here, |
| `</div>{:xml}` | end of the subdivision, |
| `</div>{:xml}` | end of the work. |


While `<div>{:xml}` elements may nest as shown here, `<p>{:xml}` elements may not. They must be encoded sequentially, i.e. as siblings.
## 3.4 Paragraph text: &lt;p&gt;

The basic-level element for prose text is the paragraph, `<p>{:xml}`. Typically, the deepest level `<div>{:xml}` element will contain one or more `<p>{:xml}` elements:

| Elements | Contents |
|-----|-----|
| `<div>{:xml}` | A new chapter starts here, |
| `<head>{:xml}` . . . `</head>{:xml}` | this contains the heading, |
| `<p>{:xml}` . . . `</p>{:xml}` | first paragraph, |
| `<p>{:xml}` . . . `</p>{:xml}` | second paragraph, |
| `<p>{:xml}` . . . `</p>{:xml}` | third paragraph, |
| `</div>{:xml}` | the chapter ends here. |


The `<p>{:xml}` element may appear in other contexts, such as in the `<teiHeader>{:xml}` element. It may also contain a number of other elements, but – as underlined above – it may not contain other `<p>{:xml}` elements, i.e. it is not allowed to nest.
## 3.5 Sentences: &lt;s&gt;

The text within a paragraph may be divided into sentences, `<s>{:xml}`:

| Elements | Contents |
|-----|-----|
| `<p>{:xml}` | A new paragraph starts here, |
| `<s>{:xml}` . . . `</s>{:xml}` | first sentence, |
| `<s>{:xml}` . . . `</s>{:xml}` | second sentence, |
| `<s>{:xml}` . . . `</s>{:xml}` | third sentence, |
| `</p>{:xml}` | the paragraph ends here. |


Paragraphs are usually not divided into sentences, but will simply list the words in sequential order. However, texts that are going to be annotated for syntax should preferably be divided into sentences according to a suitable definition of what constitutes a sentence. Since the subject is not an obligatory constituent in Old Norse, a single, finite predicate may be regarded as the basic sentence criterion, e.g. <em>Rignir</em> ‘It rains’. In the case of sentences linked by conjunctions (such as <em>ok</em>, <em>en</em> and <em>eða</em>) one might decide to declare a new sentence whenever there is a subject as well as a predicate following the conjunction. See [Haugen and Øverland 2014](HB3_literature.xml#Guidelines2014), pp. 65–67, for a discussion of sentence boundaries in Old Norwegian.
## 3.6 Words: &lt;w&gt;

Although words need not be delimited by anything else than white space, we recommend
     placing each word in a `<w>{:xml}` element. This will be helpful for word constituent analysis
     as well as for any morphological (and lexicographical) annotation.

| Elements | Contents |
|-----|-----|
| `<p>{:xml}` | A new paragraph starts here, |
| `<w>{:xml}` . . . `</w>{:xml}` | first word, |
| `<w>{:xml}` . . . `</w>{:xml}` | second word, |
| `<w>{:xml}` . . . `</w>{:xml}` | third word, |
| `</p>{:xml}` | the paragraph ends here. |


If the text is going to be annotated for morphology it is necessary to establish the exact word boundaries. The `<w>{:xml}` element may be given several morphological attributes, in particular `@lemma{:sh}` for the dictionary entry and `@me:msa{:sh}` for the grammatical form. These attributes are discussed in [ch. 11.2](HB3_ch11.xml#sec11.2) and [ch. 11.3](HB3_ch11.xml#sec11.3) below.

Another motivation for delimiting words by the `<w>{:xml}` element is the fact that a number of graphical words (i.e. sequences of characters with space on either side) should be split or merged according to standard dictionaries, e.g. “aveiðiskap” for “á veiðiskap” and “veiði kona” for “veiðikona”. Splitting and merging of graphical words will be discussed in [ch. 5.3](HB3_ch5.xml#sec5.3) below. The encoding of word constituents will also be discussed here.

If the text is transcribed on a single level, typically the diplomatic level, no specification within the `<w>{:xml}` element needs to be made. If, however, the transcription comprises more than one level of transcription, each level should be clearly marked. It is recommended, but not strictly necessary, to place these levels within a `<choice>{:xml}` element (cf. [ch. 3.13](#sec3.13) below). In a multi-level encoding, each `<w>{:xml}` element thus includes several other elements, `<choice>{:xml}` for stating the fact that the contents of this element are alternatives, `<me:facs>{:xml}` for rendering on a facsimile level, `<me:dipl>{:xml}` for rendering on a diplomatic level, and `<me:norm>{:xml}` for rendering on a normalised level. See [ch. 4.6](HB3_ch4.xml#sec4.6) for examples of multi-level encoding. (The “me:” part of the latter element name means that they are part of the name space established for Menota, as explained in [ch. 2.8](HB3_ch2.xml#sec2.8) above.)

Hyphenation of words is discussed in [ch. 5.5](HB3_ch5.xml#sec5.5) below.
## 3.7 Punctuation: &lt;pc&gt;

We recommend that punctuation characters are encoded in the same manner as words.

| Elements | Contents |
|-----|-----|
| `<w>{:xml}` . . . `</w>{:xml}` | one word, |
| `<w>{:xml}` . . . `</w>{:xml}` | another word, |
| `<pc>{:xml}` . . . `</pc>{:xml}` | a punctuation mark, |
| `<w>{:xml}` . . . `</w>{:xml}` | a new word appears here, |
| `<w>{:xml}` . . . `</w>{:xml}` | and one more. |


If the text is transcribed on a single level, typically the diplomatic level, no specification within the `<pc>{:xml}` element needs to be made. However, if the transcription comprises more than one level of transcription, each level should be clearly marked. The procedure is identical to the one specified for words above. See [ch. 5.4](HB3_ch5.xml#sec5.4) below for more details.
## 3.8 Metrical text: &lt;lg&gt; and &lt;l&gt;

The elements discussed here are defined and explained in the TEI P5 Guidelines, [ch. 6](http://www.tei-c.org/release/doc/tei-p5-doc/en/html/VE.html).

Texts in verse should be encoded using `<lg>{:xml}` (line group), which in turn contains one or more `<l>{:xml}` elements (lines). As with `<div>{:xml}`, `<lg>{:xml}` elements can nest. According to the TEI Guidelines, `<lg>{:xml}` is a sibling of, i.e. at at the same level as, `<p>{:xml}`, and cannot be contained within it (unless it appears within a `<q>{:xml}` element). Example:

| Elements | Contents |
|-----|-----|
| . . . `</p>{:xml}` | A paragraph ends here, |
| `<lg>{:xml}` | a line group starts here, |
| `<l>{:xml}` . . . `</l>{:xml}` | first line, |
| `<l>{:xml}` . . . `</l>{:xml}` | second line, |
| `<l>{:xml}` . . . `</l>{:xml}` | third line, |
| `</lg>{:xml}` | the line group ends here, |
| `<p>{:xml}` . . . | and a new paragraph starts here. |


Nesting of `<lg>{:xml}` elements is useful for marking up longer poems. When a poem consists of two levels of line groups one may encode its structure as shown here:

| Elements | Contents |
|-----|-----|
| `<lg type="stanza">{:xml}` | Here a line group on level one begins, a stanza, |
| `<lg type="couplet">{:xml}` | here a subgroup starts, a couplet, |
| `<l>{:xml}` . . . `</l>{:xml}` | the first line, |
| `<l>{:xml}` . . . `</l>{:xml}` | second line, |
| `</lg>{:xml}` | and here the subgroup ends, the first of the couplets. |
| `<lg>{:xml}` | Here a new subgroup starts, |
| `<l>{:xml}` . . . `</l>{:xml}` | line, |
| `<l>{:xml}` . . . `</l>{:xml}` | line, |
| `</lg>{:xml}` | here the second subgroup ends, |
| `</lg>{:xml}` | and here the level one line group ends. |


The `<lg>{:xml}` and `<l>{:xml}` elements may have several attributes, among other things for encoding information about rhyme or other metrical phenomena. See [ch. 13](HB3_ch13.xml) of this handbook for a more detailed presentation of metrical encoding.

Having `<p>{:xml}` and `<lg>{:xml}` as siblings can create problems for the encoding of
     prosimetrum texts, where lines or verse or even whole poems can appear within prose text, often
     as part of direct speech. However, rather than including `<lg>{:xml}` directly within the
      `<p>{:xml}` element (which would not be valid), we recommend inserting the `<p>{:xml}` and
      `<lg>{:xml}` elements within `<div>{:xml}` elements, using one `<div>{:xml}` for each of
     them:

| Elements | Contents |
|-----|-----|
| `<div type="chapter" n="1">{:xml}` | A chapter opens here, |
| `<div type="text">{:xml}` | beginning with some prose text, indicated by a `<div>{:xml}` element. |
| `<p>{:xml}` . . . `</p>{:xml}` | The text goes here, |
| `</div>{:xml}` | and ends here, indicated by the `<div>{:xml}` element. |
| `<div type="stanza">{:xml}` | Then a poem begins, indicated by a new `<div>{:xml}` element |
| `<lg>{:xml}` | with a linegroup (a stanza) |
| `<l>{:xml}` . . . `</l>{:xml}` | containing some lines. |
| `</lg>{:xml}` | The linegroup ends here, |
| `</div>{:xml}` . . . | and the poem (i.e. the `<div>{:xml}` element) also ends here. |
| `<div type="text">{:xml}` | A new piece of prose text begins, indicated by a new `<div>{:xml}` element. |
| `<p>{:xml}` . . . `</p>{:xml}` | The text goes here, |
| `</div>{:xml}` | and ends here, indicated by the `<div>{:xml}` element. |
| `</div>{:xml}` | The chapter ends here. |

## 3.9 Headings: &lt;head&gt;

The element `<head>{:xml}` is used for containing headings on all levels of the document.

| Elements | Contents |
|-----|-----|
| `<div>{:xml}` | Here a chapter begins, |
| `<head>{:xml}` . . . `</head>{:xml}` | its heading, |
| `<p>{:xml}` . . . `</p>{:xml}` | the first paragraph of the chapter, |
| `<p>{:xml}` . . . `</p>{:xml}` | the second paragraph, |
| `</div>{:xml}` | and here the chapter ends. |


The level of a heading follows
from the enclosing element. A `<head>{:xml}` element within a level
three `<div>{:xml}` element, is a heading for a level three partition
of the text.

It is not required that a `<div>{:xml}` contains a head, but if the `<head>{:xml}` element is being used within a `<div>{:xml}`, it should be the first element, followed by one or more `<p>{:xml}` (or `<lg>{:xml}`) elements. There can only be one `<head>{:xml}` element within a `<div>{:xml}`:
```xml
<div>
  <head>. . .</head>
  <p>. . .</p>
  <p>. . .<p>
</div>
```


From a logical point of view, a heading is located at the beginning of a new chapter. If one accepts that the heading may be located on the same line as the end of the previous chapter, this is not uncommon in manuscripts. One should not expect headings in manuscripts to be set out with blank lines above and below as in modern print.



<Figure>
<AutoImage src="/images/hb3/ch3/HolmPerg-34-4to-10v,1-10.jpg" alt="ch3/HolmPerg-34-4to-10v,1-10.jpg"/>
</Figure>


<Caption title="Fig. 3.1">. A chapter heading. _Landslǫg Magnúss
               Hákonarsonar_. Holm perg 34 4to, f. 10v, l. 1–10.</Caption>

In fig. 3.1, the heading, which is written in red, is located after the end of one chapter and at the beginning of a new chapter.
               The sequence of elements is thus straight-forward (beginning with the 5th line in the illustration):
```xml
<div>
   . . .
  <p>
    raðe litizt annat loglegare
  </p>
</div>
<div type="chapter">
<head type="rubric">Um grið a frosta þingi Capitulus</head>
  <p>
    Aller þeir menn sem i frosto þings logum oc for ero skulu i griðum
  </p>
    . . . 
</div>
```


The fact that the heading is a rubric has been encoded by the `@type="rubric"{:sh}` attribute.

Unfortunately (from the point of view of the encoder) many manuscripts locate headings so that they are in conflict with this simple sequencing, for example by positioning them at the end of more than one line, or even in the middle of a line containing the beginning of one chapter and the end of the previous chapter. Examples of these complications are discussed in some detail in [ch. 16.3](HB3_ch16.xml#sec16.3) below.
## 3.10 Initials and highlighted characters: &lt;c&gt;

As stated in [ch. 3.3](#sec3.3) above, the structuring of a medieval manuscript into chapters is marked up by  `<div>{:xml}` elements. Each chapter is usually indicated by an initial, often decorated and in colour, and a heading, a rubric, typically in red ink, as illustrated in fig. 3.2.



<Figure>
<AutoImage src="/images/hb3/ch3/AM-619-4to-47r,15-24.jpg" alt="ch3/AM-619-4to-47r,15-24.jpg"/>
</Figure>


<Caption title="Fig. 3.2">. Initials and highlighted characters. _The Old Norwegian Homily Book_. AM 619 4to, f. 47r, l. 15–24.</Caption>

The initial “S” marks the opening of a new homily, while the emphasised characters mark divisions at a lower level, such as the  “Ð” in the 4th line of the image, the “H” in the 5th line, etc. An initial as well as an emphasised character (<em>littera notabilior</em>) should be encoded with the `<c>{:xml}` element.

The `<c>{:xml}` element is discussed in [ch. 5.2.3](HB3_ch5.xml#sec5.2.3) below, while [ch. 7](HB3_ch7.xml) explains the encoding of graphical elements of initials.

See also [Kirsten Berg 2010](HB3_literature.xml#Berg2010), pp. 60–66, for a discussion of initials and highlighted characters in the structuring of a manuscript text like the _Old Norwegian Homily Book_.
## 3.11 Direct speech and quotations: &lt;q&gt; and &lt;quote&gt;

Dialogue are usually set out by quotation marks in edited copy, and in the encoding of a text, we recommend that the `<q>{:xml}` element should be used for identifying dialogue. Medieval manuscripts did not use quotation marks, so adding quotation marks is an editorial intervention which basically belongs to the normalised level of text encoding.

Quotation from other sources should be tagged with the `<quote>{:xml}` element. This is obviously another type of editorial intervention.

| Element | Contents |
|-----|-----|
| `<q>{:xml}` | Contains a part of a dialogue, typically in the form of direct speech. |
| `<quote>{:xml}` | Contains a quotation from another source. |


A quotation mark can be regarded as a type of punctuation character, `<pc>{:xml}`, but since quotation marks are displayed in so many different ways, we suggest that it is convenient to use an element of its own for quotation marks. See the discussion and examples in [ch. 5.6](HB3_ch5.xml#sec5.6) below.

In some cases, there may be a structural conflict between the opening and closing of a `<q>{:xml}` element, such as in this example, if it were to be encoded for `<s>{:xml}` as well as for `<q>{:xml}`:

- Eyvindr sagðisk eigi mundu brott undan ríða, “því at ek veit eigi hverir þessir eru. Mundi þat mǫrgum manni hlǿgiligt þykkja ef ek renn at ǫllu úreyndu.” (_Hrafnkels saga Freysgoða_, ch. 18)

A possibe work-around is to use the general empty element `<milestone/>{:xml}` with the obligatory `@unit{:sh}` attribute and a `@rendition{:sh}` attribute specifying the opening and closing quotation mark:
```xml
<s>Eyvindr sagðisk eigi mundu brott undan ríða, 
<milestone unit="turn" rendition="opening-quote"/>því at ek veit eigi hverir þessir eru.</s>
<s>Mundi þat mǫrgum manni hlǿgiligt þykkja ef ek renn at ǫllu úreyndu.</s><milestone 
unit="turn" rendition="closing-quote"/>
```


There are several other ways of solving this type of conflict, as we discuss at some length in [ch. 16](HB3_ch16.xml#sec5.6) below. See also the TEI P5 Guidelines, [ch. 20](https://www.tei-c.org/release/doc/tei-p5-doc/en/html/NH.html).
## 3.12 Page, column and line beginnings: &lt;pb/&gt;, &lt;cb/&gt;, &lt;lb/&gt;
### 3.12.1 Page beginnings and column beginnings

TEI uses the empty element `<pb/>{:xml}` to indicate a page beginning. This element has an attribute `@n{:sh}` which can be used for the page numbers. As it is customary to refer to the manuscript leaves, rather than pages, the value of the `@n{:sh}` attribute should indicate front or back pages (recto, verso), e.g. 1r, 1v, 2r, 2v, 3r, 3v, and so on. Column beginnings, `<cb/>{:xml}`, should also be indicated in manuscripts with two or more columns. Recommended values for the `@n{:sh}` attribute of the `<cb/>{:xml}` element are “A”, “B” and so on. For texts that are going to be displayed in the Menota archive, it is necessary to add an **ed="ms"** attribute to specify that the page and column beginning refer to the manuscript (and not to e.g. an edition of the text). Example:

| Elements | Contents |
|-----|-----|
| `<pb ed="ms" n="1r"/>{:xml}` | Folio one, recto page, begins here, |
| `<cb ed="ms" n="A"/>{:xml}` | the first column begins here, |
| `<cb ed="ms" n="B"/>{:xml}` | and the second column begins here. |
| . . . |
| `<pb ed="ms" n="1v"/>{:xml}` | Folio one, verso page, begins here, |
| `<cb ed="ms" n="A"/>{:xml}` | the first column of the verso page begins here, |
| `<cb ed="ms" n="B"/>{:xml}` | and the second column begins here. |


Page beginning information from, for example, a printed standard edition, can be encoded in addition to the `<pb ed="ms"/>{:xml}` elements that refers to the manuscript itself. If one for example would like to add page beginning information from a standard edition, one should use the `@ed{:sh}` attribute with a suitable value:
```xml
<pb ed="Standard Edition" n="1"/>
```


Some of the texts in the Menota archive have been encoded with more than one document structure, such as <em>Konungs skuggsjá</em> in AM 243 bα fol. In addition to the document structure of the manuscript, **ed="ms"**, it has the Arnamagnæan pagination, **ed="AM"**, the one from [Finnur Jónsson’s 1920–1921 edition](HB3_literature.xml#Jónsson1920-1921), **ed="FJ_utg"**, and the one from [Holm-Olsen’s 1945 edition](HB3_literature.xml#Holm-Olsen1983), **ed="H-O"**.

The relevant editions should be declared in the header. As a rule, we suggest that the edition is identified by the initials of the editor + the year of publication, e.g. FJ1920-1921, LHO1945, etc.
### 3.12.2 Line beginnings

Line beginnings are also indicated with an empty element, the `<lb/>{:xml}`, which is placed at the beginning of a new line and may be numbered by using the `@n{:sh}` attribute. For texts that are going to be displayed in the Menota archive, it is necessary to add an **ed="ms"** attribute to specify that the line beginnings refer to the manuscript (and not to e.g. an edition of the text):
```xml
<lb ed="ms" n="1"/>Line number one begins here.
```


Each page, column and line should be identified with an element at the very beginning. So for a manuscript with two columns, the three first lines in the first column on the back of the third leaf (folio) would be encoded in this manner:
```xml
<pb ed="ms" n="3v"/>
<cb ed="ms" n="A"/>
<lb ed="ms" n="1"/>This is the first line.
<lb ed="ms" n="2"/>This is the second line.
<lb ed="ms" n="3"/>This is the third line.
etc.
```


In other words, there should be as many `<pb/>{:xml}` elements as there are pages, as many `<cb/>{:xml}` elements as there are columns, and as many `<lb/>{:xml}` elements as there are lines. We strongly discourage the use of the `<lb/>{:xml}` element in the same way as the `<br>{:xml}` element in HTML, in which there typically is one `<br>{:xml}` element less than the number of lines (as the `<br>{:xml}` element is inserted between the lines).

As discussed above, one may include more than one layer of line beginnings, for example one according to the manuscript and another according to an edition, distinguishing them from each another with the `@ed{:sh}` attribute.

It is not uncommon for a word to be continued on the next line, with or without a hyphenation mark in the margin. In a single-level encoding, the `<lb/>{:xml}` element will be located within the relevant `<w>{:xml}` element, and in a multi-level encoding it will be repeated for each level. See examples of this in [ch. 5.5](HB3_ch5.xml#sec5.5) below.
## 3.13 Alternative encoding: &lt;choice&gt;

The `<choice>{:xml}` element should be used whenever the encoded text string offers parallel readings. In the context of Menota encoding, there are three major instances of this, (1) words transcribed on more than one level,  (2) the combination of an `<orig>{:xml}` and a `<reg>{:xml}` element, and (3) the combination of a `<sic>{:xml}` and a `<corr>{:xml}` element.

The first type is what we refer to as multi-level transcription, and it is discussed in a number of chapters, in particular [ch. 4.6](HB3_ch4.xml#sec4.6) below. The second type is discussed and exemplified in [ch. 9.2.3](HB3_ch9.xml#sec9.2.3) below, while the third type is treated in [ch. 9.3.3](HB3_ch9.xml#sec9.3.3).
## 3.14 Scribal hands: &lt;handShift/&gt;

While some manuscripts are written in a single hand throughout, other manuscripts are the product of two or more scribes. It is not a requirement that the transcriber identifies changes of hands, since it can be demanding to decide the number of hands and the location of the breaks between them. However, a transcriber may want to add information on hands in the document, either based on existing research or the transcriber’s own observations.

Since a shift of hands in most cases will conflict with other divisions of the text, the recommended element is an empty one, `<handShift/>{:xml}`. This is an element of the same type as the ones discussed in [ch. 3.12](#sec3.12) above. If there are two hands in a document there will thus be a single `<handShift/>{:xml}` element, located at the break between hand 1 and hand 2.

Information on the hands of a document can be given as part of the header in the `<handDesc>{:xml}` element. See [ch. 14.3.3](HB3_ch14.xml#sec14.3.3) below.