---
title: "Menota handbook appendix H (v. 3.0): Stylesheets (XSL and CSS) for the handbook"
author:
 - Marco Bianchi
 - 
changeLog:
 - date: 2026-03-10
   author: Robert K. Paulsen
   changes:
    - Converted from XML to MDX.
---


# Appendix H: Stylesheets (XSL and CSS) for the handbook

Version 3.0 (12 December 2019)

by Marco Bianchi

## H.1 Introduction

Version 3 of the Menota handbook is transformed to html with the stylesheet
               handbook_v3.xsl. The stylesheet outputs clean box-based html5. Generally, all
                  `@rend{:sh}` are converted to html classes. All styling is taken care of by
               menota_handbook_3.css.

## H.2 The HTML

handbook_v3.xls produces clean html5 without any form styling in html code. The
               html skeleton:
```xml
<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <div id="page">
      <div id="main-wrapper">
        <div id="header-wrapper">
          <header> </header>
          <nav> Site-navigation</nav>
        </div>
        <div id="content-wrapper">
          <section id="sidebar" role="navigation">Page-MENU</section>
          <main id="content">
            <article>HANDBOOK ENTRIES</article>
          </main>
        </div>
      </div>
      <footer></footer>
    </div>
  </body>
</html>
```


## H.3 The CSS

The css sheets use css3 but should degrade gracefully in non-compliant browsers. The
               main typeface for handbook v3 is Andron, which is read in with css
                  `@font-face{:sh}` and thus does not need to be installed on users’ computers.
                  `@font-face{:sh}` is supported in all major modern browsers. Non compliant
               browsers will display a serif-font. 

Handbook v3 uses one css sheet: menota_handbook_3.css

Breaking points for small screens are set at 960px (width of header items and main
               content 100%) and 450px in portrait mode (larger main font-size).

## H.4 XML markup

In the tables below supported xml markup and its corresponding html output is listed.
               All values of `@rend{:sh}` and `@xml:id{:sh}` will be passed to html
                  `@class{:sh}` and `@id{:sh}` respectively, unless otherwise noted.

### H.4.1 CSS Block elements

<div className="table-xml-elements">
| tei:xml | html | function | Comment |
|-----|-----|-----|-----|
| code | `<pre>{:xml}``<code>{:xml}` | Code example | [See H.7 on styling of code](#secH.7) |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| <span className="p-6"/>`"inline"{:js}` |  | Inline code example |  |
| `<eg>{:xml}` | `<pre>{:xml}``<code>{:xml}` | Example of code | [See H.7 on styling of code](#secH.7) |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| <span className="p-6"/>`"inline"{:js}` |  | Inline code example |  |
| `<egXML>{:xml}` | `<pre>{:xml}``<code>{:xml}` | Example of xml code | [See H.7 on styling of code](#secH.7) |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| <span className="p-6"/>`"inline"{:js}` |  | Inline code example |  |
| `<figure>{:xml}` | `<figure>{:xml}` | Illustration | Typically, a `<figure>{:xml}` element contains a `<graphic>{:xml}` and a `<figDesc>{:xml}` element. [See H.6 on styling of images](#secH.6) |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| <span className="p-6"/>`"leftalign"{:js}` |  |  | The figure is placed to the left, with text floating to the right |
| <span className="p-6"/>`"rightalign"{:js}` |  |  | The figure is placed to the right, with text floating to the left |
| <span className="p-6"/>`"center"{:js}` |  |  | The figure is placed in the center. No floating occurs. |
| `<figure>{:xml}`/`<figDesc>{:xml}` | &lt;figcaption span class="imagelabel"&gt; | Figure caption | Should be used together with a sibling `<graphic>{:xml}` and a parent `<figure>{:xml}` |
| <span className="p-3"/>`@n{:sh}` |  |  |  |
| <span className="p-6"/>`"[number]"{:js}` | &lt;figcaption&gt;&lt;span class="imagelabel"&gt;Fig. &lt;span class="imageid"&gt;2.1&lt;&gt;/span&gt;.&lt;/span&gt;Caption&lt;/figcaption&gt; |  | Usage in xml: `&lt;figDesc n="2.1"&gt;Caption&lt;/figDesc&gt;`, i.e. the figure’s number should passed through `@n{:sh}` and not be put between the `<figDesc>{:xml}` tags |
| `<graphic>{:xml}` | `<img>{:xml}` | Image | If used together with a parent element `<figure>{:xml}`, the image element will be wrapped in a `<figure>{:xml}` element together with its caption. It may, however, also be used alone. |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| `<head>{:xml}` | `<h1>{:xml}`, `<h2>{:xml}`, `<h3>{:xml}`, `<h4>{:xml}`, `<head>{:xml}` | Heading | `<h1>{:xml}`–`<h4>{:xml}` are chosen according to their position in the dom. If the position does not match, a generic div class="head" is output.  |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| <span className="p-3"/>`@xml:id{:sh}` |  |  | When no `@xml:id{:sh}` is passed, the xsl will introduce an `@id{:sh}` with a random value |
| `<l>{:xml}` | `<li>{:xml}` | List item |  |
| `<lg>{:xml}` | `<ul>{:xml}` | Unordered list |  |
| `<list>{:xml}` | `<ul>{:xml}` | List | Unordered list |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| <span className="p-6"/>`"numbered"{:js}` | `<ol>{:xml}` | Ordered list | Ordered (= numbered) list |
| <span className="p-3"/>`@type{:sh}` |  |  | <span className="p-3"/>`@type{:sh}` is converted to a css `@class{:sh}` for backwards compatibility with earlier style sheets |
| <span className="p-3"/>`@xml:id{:sh}` |  |  |  |
| `<list>{:xml}`/`<item>{:xml}` | `<li>{:xml}` | List item |  |
| `<p>{:xml}` | `<p>{:xml}` | Paragraph |  |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| <span className="p-6"/>`"sample"{:js}` | `<blockquote>{:xml}` | Blockquote |  |
| <span className="p-3"/>`@xml:id{:sh}` |  |  |  |
| `<quote>{:xml}` | `<blockquote>{:xml}` | Blockquote |  |
| `<table>{:xml}` | `<table>{:xml}` | Table | [See H.5 on styling of tables](#secH.5) |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| <span className="p-6"/>`"xml-elements"{:js}` |  |  | This value can be used for tables with lists of xml-elements and their attributes. [See H.5.1 for more information](#secH.5.1) |
| <span className="p-6"/>`"abbr"{:js}` |  |  | This value can be used for tables with explanations of abbreviations and other glyphs. [See H.5.2 for more information](#secH.5.2) |
| <span className="p-3"/>`@xml:id{:sh}` |  |  |  |
| `<table>{:xml}`/`<row>{:xml}` | `<tr>{:xml}` | Table row |  |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| <span className="p-3"/>`@xml:id{:sh}` |  |  |  |
| <span className="p-3"/>`@role{:sh}` |  |  |  |
| <span className="p-6"/>`"label"{:js}` | `<tr>{:xml}`/`<th>{:xml}` | Table header | i.e. table header |
| `<table>{:xml}`/`<cell>{:xml}` | `<td>{:xml}` or `<th>{:xml}` | Table cell | `<th>{:xml}` when the parent `<row>{:xml}``@role{:sh}`=`"label"{:js}` |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
</div>


### H.4.2 CSS Inline elements

<div className="table-xml-elements">
| tei:xml | html | Function | Comment |
|-----|-----|-----|-----|
| `<att>{:xml}` | `&lt;span class="attribute"&gt;&lt;code&gt;@att&lt;/code&gt;&lt;/span&gt;` | xml/html attribute |  In a table with `@rend{:sh}`=`"xml-elements"{:js}` attributes in the first columns are automatically indented 1em. [See H.7 on styling of code](#secH.7).  |
| `<emph>{:xml}` | `<em>{:xml}` | Emphasis |  |
| `<gi>{:xml}` | `&lt;span class="tag"&gt;&lt;code&gt;@att&lt;/code&gt;&lt;/span&gt;` | xml/html element | [See H.7 on styling of code](#secH.7) |
| `<hi>{:xml}` | `<span>{:xml}` | Highlighted |  |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| <span className="p-6"/>`"bold"{:js}` | `<b>{:xml}` |  | If `@rend{:sh}` contains multiple values, `"bold"{:js}` is passed to `&lt;span class="bold [othervalue]"&gt;` |
| <span className="p-6"/>`"code"{:js}` | `<code>{:xml}` |  | If `@rend{:sh}` contains multiple values, `"code"{:js}` is passed to `&lt;span class="code [othervalue]"&gt;`[See H.7 on styling of code](#secH.7) |
| <span className="p-6"/>`"codepoint"{:js}` | &lt;span class="codepoint"&gt;&lt;code&gt;U+01eb&lt;/code&gt;&lt;/span&gt; | A Unicode codepoint: `U+01eb` | Lowercase letters are transformed to uppercase letters in the css stylesheet. |
| <span className="p-6"/>`"descName"{:js}` | &lt;span class="descname"&gt;&lt;code&gt;LATIN SMALL LETTER O WITH OGONEK&lt;/code&gt;&lt;/span&gt; | A Unicode descriptive name: `LATIN SMALL LETTER O WITH OGONEK` | Lowercase letters are transformed to uppercase letters in the css stylesheet. |
| <span className="p-6"/>`"entity"{:js}` | &lt;span class="entity"&gt;&lt;code&gt;&amp;oogon;&lt;/code&gt;&lt;/span&gt; | A Unicode entity: `&oogon;{:xml}` |  |
| <span className="p-6"/>`"glyph"{:js}` | &lt;span class="glyph"&gt; |  | Characters marked upp with `"glyph"{:js}` are displayed in a deviant color. See also the section about [styling tables](#secH.5.2). |
| <span className="p-6"/>`"italic"{:js}` | `<i>{:xml}` |  | If `@rend{:sh}` contains multiple values, `"italic"{:js}` is passed to `&lt;span class="italic [othervalue]"&gt;` |
| <span className="p-6"/>`"red"{:js}` | &lt;span class="red"&gt; |  |  |
| <span className="p-6"/>`"blue"{:js}` | &lt;span class="blue"&gt; |  |  |
| <span className="p-6"/>`"initial"{:js}` | &lt;span class="initial"&gt; |  |  |
| `<lb/>{:xml}` | `<br/>{:xml}` | Line break |  |
| `<q>{:xml}` | `<q>{:xml}` | Quoted |  |
| `<ref>{:xml}` | `<a>{:xml}` | Reference |  |
| <span className="p-3"/>`@target{:sh}` | <span className="p-3"/>`@href{:sh}` |  |  |
| <span className="p-3"/>`@rend{:sh}` |  |  |  |
| `<tag>{:xml}` | `&lt;code&gt;&lt;span class="tag"&gt;@att&lt;/span&gt;&lt;/code&gt;` | Tagged content | [See H.7 on styling of code](#secH.7) |
| `<title>{:xml}` | `<cite>{:xml}` | Title of a work |  |
| `<val>{:xml}` | `&lt;span class="value"&gt;'value'&lt;/span&gt;` | Value of a xml/html element | [See H.7 on styling of code](#secH.7). In a table with `@rend{:sh}`=`"xml-elements"{:js}` values in the first columns are automatically indented 2em |
</div>


### H.4.3 Generic values for `@rend{:sh}`

The following `@rend{:sh}` values can be given to any xml-element with support
                  for `@rend{:sh}`:

- `"blue"{:js}` (blue font color)
- `"bold"{:js}` (bold style)
- `"code"{:js}` (monospace font)
- `"red"{:js}` (red font color)
- `"style"{:js}` (italic style)

The following `@rend{:sh}` values can be given to any xml-element that
                  translates into a block-element in html/css and supports the
                  `@rend{:sh}` attribute. Percentages are always relative to the width of the
                  parent element’s width. The available values for `@rend{:sh}` behave the
                  following way at different screen resolutions:

| `@rend{:sh}`=... | default | medium screens | small screens |
|-----|-----|-----|-----|
| `"alignleft"{:js}` | align:left |  |  |
| `"alignright"{:js}` | align:right |  |  |
| `"aligncenter"{:js}` | centered |  |  |
| `"center"{:js}` | text-align:center |  |  |
| `"clearboth"{:js}` | float:none; clear:both (forces the element to be placed on a new line, i.e surrounding text will not float around the element) |  |  |
| `"width10"{:js}` | width:10% | `<figure>{:xml}` width:50% | `<figure>{:xml}` width:100% |
| `"width20"{:js}` | width:20% | `<figure>{:xml}` width:50% | `<figure>{:xml}` width:100% |
| `"width25"{:js}` | width:25% | `<figure>{:xml}` width:50% | `<figure>{:xml}` width:100% |
| `"width30"{:js}` | width:30% | `<figure>{:xml}` width:50% | `<figure>{:xml}` width:100% |
| `"width33"{:js}` | width:33,33% | `<figure>{:xml}` width:50% | `<figure>{:xml}` width:100% |
| `"width40"{:js}` | width:40% | `<figure>{:xml}` width:50% | `<figure>{:xml}` width:100% |
| `"width50"{:js}` | width:50% |  | `<figure>{:xml}` width:100% |
| `"width60"{:js}` | width:60% | `<figure>{:xml}` width:100% | `<figure>{:xml}` width:100% |
| `"width66"{:js}` | width:66,66% | `<figure>{:xml}` width:100% | `<figure>{:xml}` width:100% |
| `"width70"{:js}` | width:70% | `<figure>{:xml}` width:100% | `<figure>{:xml}` width:100% |
| `"width75"{:js}` | width:75% | `<figure>{:xml}` width:100% | `<figure>{:xml}` width:100% |
| `"width80"{:js}` | width:80% | `<figure>{:xml}` width:100% | `<figure>{:xml}` width:100% |
| `"width90"{:js}` | width:90% | `<figure>{:xml}` width:100% | `<figure>{:xml}` width:100% |
| `"width100"{:js}` | width:100% | `<figure>{:xml}` width:100% | `<figure>{:xml}` width:100% |


## H.5 Styling tables

Tables are structured according to the following pattern in html:
```xml
<table>
  <colgroup>
    <col style="width20">
    <col style="width80">
  </colgroup>
  <thead>
    <tr>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td></td>
      <td></td>
    </tr>
  </tbody>
</table>
```


The `<colgroup>{:xml}` element is created dynamically if the first row contains cells
               with the attribute `@rend{:sh}`. This means that the column widths of the
               html table can be controlled with the attribute `@rend{:sh}` in the cells of the
               first table row. Below is the xml code of the html table above:
```xml
<table>
  <row rend="label">
    <cell rend="width20"></cell>
    <cell rend="width80"></cell>
  </row>
  <row>
    <cell></cell>
    <cell></cell>
  </row>
</table>
```


### H.5.1 Styling tables with explanations of xml-elements

A table with explanations of xml-elements and their attributes ([such as H.4.1 above](#tab.1)) can be given
                     `@rend{:sh}`=`"xml-elements"{:js}` in the `<table>{:xml}` element. The
                  xls stylesheet tries to wrap all rows with a `<gi>{:xml}` or a tag in
                  the first cell into a `<tbody>{:xml}` element together with the following rows.
                  Such tables must adhere to the following principles: 

- The `<table>{:xml}` tag must have a `@rend{:sh}` with the value `"xml-elements"{:js}`
- The first cell of the first row in each block must contain a `<gi>{:xml}` or a `<tag>{:xml}` element
- The first cells of all other rows in a block must not contain any `<gi>{:xml}` element (otherwise, the respective row will be treated as the first row in a block)

Strings marked with `<att>{:xml}` and `<val>{:xml}` in the first cell of each row
                  are indented automatically.

### H.5.2 Styling glyph tables

Tables with explanations of abbreviations and other glyphs can be marked upp with
                     table rend="abbr". In such tables, cells containing a tag hi
                     rend="glyph" (and nothing else) are rendered differently.

## H.6 Styling images

An image and its caption should be structured as follows:
```xml
<figure rend="leftalign width33">
  <graphic url="path/to/image.jpg" />
  <figDesc n="1.2">Figure Caption</figDesc>
</figure>
```


The position and width of the image can be controlled with `<rend>{:xml}` in the
               wrapping `<figure>{:xml}` element. By default, `<figure>{:xml}` is displayed floating
               to the left of the text at a width of 50% with a border around the containing
               elements. The figure’s number should passed through `@n{:sh}` in `<figDesc>{:xml}`.
               The xslt adds automatically a figure label. The above `<figDesc>{:xml}` will thus be
               rendered as “Fig. 1.2: Figure Caption”.

If used without a wrapping `<figure>{:xml}` element, images placed in a
                  `<graphic>{:xml}` element are displayed at their original size without any
               border

## H.7 Styling code

Block level code elements are wrapped into a html `<pre>{:xml}` tag that preserves
               line breaks, white spaces and indentations. Such elements should thus not follow the
               indentation of surrounding xml. The following coding of a CDATA section with
               surrounding xml ...
```xml
<p>Some text before the code</p>
  <egXML><![CDATA[<figure rend="leftalign width33">
   <graphic url="path/to/image.jpg"/>
   <figDesc n="1.2">Figure Caption</figDesc>
   </figure>]]>
  </egXML>
<p>Some text after the code</p>
```


... will be output as:
```xml
<figure rend="leftalign width33">
  <graphic url="path/to/image.jpg" />
  <figDesc n="1.2">Figure Caption</figDesc>
</figure>
```


By default, code within `<eg>{:xml}`, `<egXML>{:xml}` and `<code>{:xml}` is displayed as
               a block element. With `@rend{:sh}`=`"inline"{:js}` it can be forced to be
               treated as an inline element. Alternatively, the markup `&lt;hi
                  rend="code"&gt;` or `<tag>{:xml}` may be used for inline code.

Xml elements, attributes and values should be tagged with their respective semantic
               markup: `<gi>{:xml}`, `<att>{:xml}` and `<val>{:xml}`.

## H.8 Table of Contents

A table of contents is added automatically in the beginning of each page. The table
               includes all second level headers.